#include "server/socket_server.h"
#include "client/socket_client.h"
#include <gtest/gtest.h>
#include <thread>

// Placeholder test so that the test suite runs
TEST(PlaceholderTest, Placeholder) {
    EXPECT_EQ(1, 1);
}